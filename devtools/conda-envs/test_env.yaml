name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - pandas
  - networkx

    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - mdtraj
    - codecov
    - point_cloud_utils
    - networkx

