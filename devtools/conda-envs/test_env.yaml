name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - pandas
  - networkx
  - point_cloud_utils
  - mdtraj
  - shapely


    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
    - geovoronoi

